AWS_REGION=us-east-1

# Google Sheets: partners are read live from the "Audit Links" tab.
# Falls back to PARTNER_CSV below if the live fetch fails.
GOOGLE_SERVICE_ACCOUNT_KEY=/path/to/service-account-key.json
PARTNER_SHEET_ID=1rlRHS_FXe0APyXZg9tR3ufnV8eN3e4otJxtrQj2Z5pw
PARTNER_SHEET_TAB=Audit Links

# Local CSV fallback (used only if the live sheet is unreachable)
PARTNER_CSV=./partner-links.csv
COMPLIANCE_RULES_CSV=./compliance-rules.csv

# Optional: local Chrome executable path (auto-detected if omitted)
# CHROME_PATH=/Applications/Google Chrome.app/Contents/MacOS/Google Chrome

# Optional: headless mode (default true; set false to watch the browser)
# HEADLESS=false
